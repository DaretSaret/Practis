Навигация по коммитам: Part1 - Часть 1 в задании, PartN - Часть N.

Задача: Разработка системы работы с черным ящиком

1)Чтение и вывод файлов:

Создать меню для чтения конфигурации (JSON) и CSV-файлов.
Вывести конфигурацию и строки данных (N до M).

2)Интерпретация данных:

Интерпретировать байты в числа на основе конфигурации.
Сохранять данные в коллекцию (рекомендуется DataTable).

3)Вывод и экспорт данных:

Добавить пункты меню для вывода полезных данных и экспорта в XLS-файл.
Реализовать соответствующие функции.

4)Проверка наличия данных:

Проверять наличие данных перед выводом пунктов меню.
Сообщать о недоступности пунктов при их выборе.;


Используемые библиотеки:
using System;
using System.Collections.Generic;
using System.Data;
using System.IO;
using System.Linq;
using System.Text.Json;
using OfficeOpenXml;
using System.Text.Json.Serialization;
using System.Globalization;
using System.Linq;


Возникшие трудности: 

1)Была маленькая сложность с определением путей в коде для поиска файлов .csv и .json;

2)".csv" файл имеет достаточное количество ошибок(пустых строк и неправильных) из-за чего программа ложилась или неправильно выводила данные;

3)Ошибки при преобразовании строк в числа или другие типы данных из ".csv", так как там присутствуют сломанные строки;

4)Мелкие сложности с поиском недостающих строк в коде для экспорта файла и работы с библиотекой EPPlus;

5)Проблема, которая так и не решилась: ".csv" файл содержит индекс [0] - Time, [1] - Id девайса и [2] - Время. Изначально к [0] было использован формат "Time" в коде из-за чего происходили ошибки при чтении файла ".csv".
Позже было изменено на string, чтобы Время (16:xx:xx) сохранялоась как строка, ошибка пропала. Индекс [2] в ".csv" файле также был изменен в коде на String, но почему-то после интерпретации данных, он не сохраняется и постоянно 0 в отладке и выводе полезных данных, в ".xlsx" та же проблема со [2] индексом из файла ".csv".
Пытался ему изменить сохранение в формат double и int. Добавлял отладку в код, при интерпретации данных в консоле выводилось Время - индекс [2] ".csv" файла, но после какого-то момента переставало.


Автор работы: Чайко Денис; 
